#!/usr/bin/env mksh
#
#
THIS_DIR="$(dirname "$(dirname "$(readlink -m "$0")")")"
action=$1; shift

set -u -e -o pipefail

# source "$THIS_DIR/bin/source/name-to-func/_.sh"
# name-to-func "${action}" "$@"

export MKSH_DIR="$THIS_DIR"
source "$THIS_DIR/bin/public/$action/_.sh"
$action "$@"
