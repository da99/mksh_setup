#!/usr/bin/env mksh
#
#
THIS_DIR="$(dirname "$(dirname "$(readlink -m "$0")")")"
action=$1; shift

set -u -e -o pipefail

source "$THIS_DIR/bin/public/name-to-func/_.sh"
name-to-func "$0" "${action}" "$@"
